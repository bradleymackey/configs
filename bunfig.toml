[test]
# `bun run test` enables coverage; these thresholds then fail the run if unit
# coverage of install/ drops.
coverageThreshold = { lines = 0.95, functions = 0.95 }
coverageSkipTestFiles = true
coveragePathIgnorePatterns = ["test/**"]
